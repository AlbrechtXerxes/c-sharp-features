namespace ModelNamespace
{
    //We have a model with namespace: 'ModelNamespace' that we want to attach in multiple files.
    //For this, we can use 'global'
    public class SomeGlobalModel
    {
        public int MyProperty { get; set; }
    }
}
