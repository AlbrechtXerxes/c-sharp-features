<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net6.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>


  <ItemGroup>
    <!--with this line below, we need to extra include using "System"-->
    <!--<Using Remove="System"/>-->
    <!--with this, we can uncomment all usings with 'ModelNamespace'-->
    <Using Include="ModelNamespace"/>
  </ItemGroup>
  
</Project>
